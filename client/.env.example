# Base origin of the deployed Dream Brain API (no trailing slash, no /api).
# Leave empty for local dev — Vite proxies /api to http://localhost:5050.
# In production (Vercel), set this to your Render backend URL:
#   VITE_API_URL=https://dream-brain-api.onrender.com
VITE_API_URL=
